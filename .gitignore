# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# build artifacts
/build
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local
.env.local

# vercel
.vercel

# IDE
.idea
.vscode

# Streetwear design iteration renders — kept local, not versioned (drafts).
# Every iteration stays reproducible via scripts/generate-design.mjs + its prompt.txt.
research/streetwear-decode/iterations/**/image-*.png
research/streetwear-decode/iterations/**/image-*.jpeg
research/streetwear-decode/iterations/**/response-*.json
research/streetwear-decode/iterations/**/image-*.webp

# Raw on-model shoot archive — heavy (~230MB of drafts), kept local only.
# The curated FINAL on-model shots live in research/first-drop/ (tracked).
research/models/

# Working/scratch subfolders inside first-drop (keep only the curated *-images/ finals).
research/first-drop/reshoot-*/
research/first-drop/pant-model-*/
research/first-drop/_*/
